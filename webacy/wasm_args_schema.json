{
  "type": "object",
  "description": "Inputs passed to the Webacy address-risk policy WASM at evaluation time",
  "properties": {
    "address": {
      "type": "string",
      "description": "Wallet or contract address (0x-prefixed) to score with the Webacy risk API."
    },
    "chain": {
      "type": "string",
      "description": "Optional Webacy chain identifier (e.g. 'eth', 'bsc', 'polygon'). Defaults to the API's default chain when omitted."
    }
  },
  "required": ["address"]
}
