{
  "type": "object",
  "properties": {
    "ACCOUNTABLE_API_KEY": {
      "type": "string",
      "description": "Optional. Accountable's per-venue and network-status endpoints are documented as public and keyless; only set this if a given deployment requires a key."
    }
  },
  "required": [],
  "additionalProperties": false
}
