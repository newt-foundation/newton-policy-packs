# Newton Policy Development — Production (Mainnet)
# Copy to .env and fill in your values:
#   cp .env.prod .env

CHAIN_ID=1
DEPLOYMENT_ENV=prod
RPC_URL=https://ethereum-rpc.publicnode.com
PRIVATE_KEY=0x

# IPFS uploads (optional — Newton proxy is used if omitted)
PINATA_JWT=
PINATA_GATEWAY=
