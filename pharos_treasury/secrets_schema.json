{
  "type": "object",
  "properties": {
    "PHAROS_API_KEY": {
      "type": "string",
      "minLength": 1,
      "description": "Pharos API key, sent as the X-API-Key request header."
    }
  },
  "required": ["PHAROS_API_KEY"],
  "additionalProperties": false
}
